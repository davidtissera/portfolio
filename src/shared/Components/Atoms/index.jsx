/**
  * ! In this file we should include only Atom components.
*/

/**
  * ? Examples of atomic components:
  * ? Buttons.
  * ? Images.
  * ? Form fields.
  * ? Chips.
  * ? Inputs.
  * ? Alert.
*/

/*
  * Atoms are the basic unit in Atomic Design.
  * and are the smallest indivisible units that make sense by themselves.
*/

import { FieldText, FieldSelect } from './FormFields';
import Button from './Button';

export {
  FieldText,
  FieldSelect,
  Button,
};