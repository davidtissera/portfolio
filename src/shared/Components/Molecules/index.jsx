/**
  * ! In this file we should include only Molecules components.
*/

/**
  * ? Examples of molecules components:
  * ? Navigation Menu.
  * ? Table
  * ? Search box.
  * ? Drag and drop.
*/

/*
  * As in chemistry, molecules are the groupings of atoms that,
  * when united, acquire new characteristics and meanings and function as a unit,
  * With the molecules we make sure that there is a graphic, structural and conceptual coherence in all the components
  * that we build in our web. This also implies that we must build elements designed to be reused in different contexts.
*/

import Table from './Table';
import NavigationMenu from './NavigationMenu';

export {
  Table,
  NavigationMenu,
};
